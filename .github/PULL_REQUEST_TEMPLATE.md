## What

<!-- What does this change do? -->

## Why

<!-- The problem it solves or the need it addresses. -->

## Verification

<!-- How you know it works: commands run, output, tests. -->

- [ ] `make check` is green (typecheck + lint + format + test)
- [ ] DB-backed tests pass where relevant (`RUN_DB_TESTS=1 npm test` against pgvector)
- [ ] Conventional commit title (`feat:` / `fix:` / `chore:` / `docs:`)

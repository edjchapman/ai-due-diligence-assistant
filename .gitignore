node_modules/
dist/
.env
*.log
coverage/

# Claude Code — ignore only the personal/machine-specific bits (symlinks + local
# settings). Deliberately NOT a blanket .claude/ ignore, so a committed
# .claude/settings.json + .claude/hooks/ bootstrap can still be tracked.
.claude/agents
.claude/commands
.claude/skills
.claude/rules
.claude/settings.local.json
.mcp.json

# JetBrains IDE
.idea/

# Personal work-logs (from the /standup skill) — kept local, off the public repo
standups/

# Added by ggshield
.cache_ggshield

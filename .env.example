# Postgres (pgvector) connection
DATABASE_URL=postgres://postgres@localhost:5432/ai_dd
PORT=3000
# OpenAI — embeddings only (text-embedding-3-small); used by `npm run ingest` and /search
OPENAI_API_KEY=
# Anthropic — reasoning agent, used from M3
ANTHROPIC_API_KEY=
